# i18n Phase 3 系統性修改清單

> 最後更新：2026-04-18
> 目標：UroSense 支援 5 國語言（zh-TW / en-US / ja-JP / ko-KR / vi-VN），解決「切英文後病患端頁面仍為中文」的回報 bug，並完成全站 i18n 漏洞掃除。
> 完成一項就把 `[ ]` 改成 `[x]`，並在後面加上完成日期與 commit hash。

---

## 原始問題與脈絡

- 使用者回報：切換英文後，「選擇症狀」與「填寫病史資料」頁仍顯示中文。
- 根本原因：
  1. 前端 intake 流程兩頁完全硬編碼中文，沒走 `useTranslation`。
  2. 後端 `chief_complaints` 只有 zh-TW 欄位，沒有多語 schema。
  3. API 無語言 resolver，也無中介層讀取 `Accept-Language`。
  4. `SupportedLanguage` 僅 zh-TW / en，沒有 ja-JP / ko-KR / vi-VN。
- 修復策略：expand-migrate-contract，不破壞 legacy 欄位；前端 namespace 拆分；beta 語言走 fallbackLng chain。

---

## 已完成（本輪）

### [x] A1 擴充 `SupportedLanguage` 至 5 語 — 2026-04-18 (84bd325)
- 新增 ja-JP / ko-KR / vi-VN 為 beta locale
- 前端 `SUPPORTED_LANGUAGES` + 後端 `LANGUAGE_MAP` 同步

### [x] A2 DB schema：`chief_complaints` 改 `*_by_lang` JSONB — 2026-04-18 (03a5f59)
- Alembic migration `a8b9c0d1e2f3`
- 新增 `name_by_lang` / `description_by_lang` / `category_by_lang` JSONB
- 自動 backfill 既有 zh-TW / en 欄位

### [x] A3 API 語言 resolver（middleware + helper） — 2026-04-18 (03a5f59)
- `LanguageMiddleware` 解析 `?lng` query、`X-Language` header、`Accept-Language`
- `pick()` helper：4 層 fallback（target → default → any non-empty → legacy）
- 14 支新單元測試（`utils/test_localized_field.py` + `core/test_language_middleware.py`）

### [x] A4 Frontend i18n namespace 拆分 — 2026-04-18 (9a7697a)
- 新增 `intake` namespace（zh-TW + en-US）
- `i18next` `ns` 陣列登記；beta locale 走 fallback chain

### [x] A9 新增語言 runbook + onboarding checklist — 2026-04-18 (03a5f59)
- `docs/runbook/add_new_language.md`

### [x] B2 SelectComplaintPage i18n — 2026-04-18 (9a7697a)
- 全頁字串改走 `t('selectComplaint.*')`
- zh-TW category 字串以 `CATEGORY_I18N_KEY` 映射成顯示 key

### [x] B3 MedicalInfoPage i18n — 2026-04-18 (9a7697a)
- 5 份常數清單（yearsAgo / frequency / relations / commonAllergies / commonConditions）改存 key
- 30+ JSX 字串改走 `t('medicalInfo.*')`

---

## P0 — 阻斷級（上線前必修）

### [ ] B1 `chief_complaints` 4 語 seed 資料
- **檔案**：
  - `backend/alembic/versions/xxxx-seed_chief_complaints_multilang.py`（新）
  - 或直接 `backend/scripts/seed_chief_complaints.py`
- **要做**：
  - [ ] 準備 10 筆主訴（既有 zh-TW 主訴）的 en-US / ja-JP / ko-KR / vi-VN 翻譯
  - [ ] 寫入 `name_by_lang` / `description_by_lang` / `category_by_lang`
  - [ ] 分類同步翻譯：`排尿症狀 / 疼痛 / 檢查異常 / 其他`
  - [ ] 加單元測試：`pick()` 每種語言都能正確 resolve
- **驗收**：
  - `GET /api/v1/complaints?lng=ja-JP` 回傳全日文
  - 缺 key 時退回 en-US 再退 zh-TW，不 500

### [ ] B9 Playwright `/en-US/*` CJK = 0 驗收
- **檔案**：`frontend/tests/e2e/i18n_en_no_cjk.spec.ts`（新）
- **要做**：
  - [ ] 走完 patient 流程（select complaint → medical info → summary）
  - [ ] 每頁截圖，用 regex `/[\u4e00-\u9fff\u3040-\u30ff\uac00-\ud7af]/` 檢查文字
  - [ ] 同樣流程 zh-TW 也跑一次當 baseline
- **驗收**：`/en-US/*` 的所有可見文字 CJK 字元數 = 0

---

## P1 — 主線（功能完整性）

### [ ] A5 Frontend dynamic locale bundle（`i18next-http-backend`）
- **檔案**：`frontend/src/i18n/index.ts`、`vite.config.ts`、`public/locales/*`
- **要做**：
  - [ ] 移除 inline import JSON，改 `HttpBackend`
  - [ ] build 時把 locale JSON 放到 `public/locales/<lng>/<ns>.json`
  - [ ] 加 loading fallback（switch 語言時不要閃一下空白）
  - [ ] `Cache-Control: public, max-age=3600` 減少重複抓取
- **驗收**：初始 bundle 只載 zh-TW；切 en-US 才 HTTP 抓 en-US 檔

### [ ] A6 字體切割載入（CJK + 越南文擴字）
- **檔案**：`frontend/src/styles/fonts.css`、`frontend/index.html`
- **要做**：
  - [ ] Noto Sans TC / JP / KR 用 `unicode-range` 切 subset
  - [ ] 越南文加 `latin-ext` subset（đ ă â ê ô ơ ư 等）
  - [ ] `font-display: swap` 避免 FOIT
- **驗收**：初始 font 下載 < 100KB；切語言時補載對應 subset

### [ ] B4 `format.ts` 動態 locale
- **檔案**：`frontend/src/utils/format.ts`
- **要做**：
  - [ ] `formatDate / formatTime / formatRelative` 改吃 `i18n.language`
  - [ ] 相對時間用 `Intl.RelativeTimeFormat`
  - [ ] 數字用 `Intl.NumberFormat`，醫療單位維持 mg/dL
- **驗收**：切英文後日期顯示 `Apr 18, 2026` 而非 `2026年4月18日`

### [ ] B5 StatusBadge + SeverityBadge i18n
- **檔案**：
  - `frontend/src/components/common/StatusBadge.tsx`
  - `frontend/src/components/common/SeverityBadge.tsx`
- **要做**：
  - [ ] 所有 `case 'active': return '進行中'` 改 `t('status.active')`
  - [ ] 新增 `common.status.*` / `common.severity.*` key
- **驗收**：Storybook / 各頁使用處英文模式下顯示英文

### [ ] B6 SOAPCard 80+ 欄位 i18n
- **檔案**：`frontend/src/components/doctor/SOAPCard.tsx` + 子元件
- **要做**：
  - [ ] 抽出所有 label（subjective / objective / assessment / plan 80+ 欄位）
  - [ ] 建 `soap` namespace
  - [ ] 醫療術語與臨床討論確認翻譯（ICD-10、解剖部位、藥名）
- **驗收**：切英文後 SOAP 報告全英；醫療術語與既有 en-US 醫學詞庫一致

### [ ] B7 Sidebar / AlertItem / TranscriptPanel i18n
- **檔案**：
  - `frontend/src/components/doctor/Sidebar.tsx`
  - `frontend/src/components/doctor/AlertItem.tsx`
  - `frontend/src/components/doctor/TranscriptPanel.tsx`
- **要做**：
  - [ ] 導航字串、alert 動作按鈕、transcript placeholder 全走 t()
- **驗收**：醫師工作台切英文後無中文

### [ ] B8 Session pages i18n（Complete / ThankYou / Detail / History）
- **檔案**：
  - `frontend/src/screens/patient/SessionCompletePage.tsx`
  - `frontend/src/screens/patient/ThankYouPage.tsx`
  - `frontend/src/screens/doctor/SessionDetailPage.tsx`
  - `frontend/src/screens/doctor/SessionHistoryPage.tsx`
- **要做**：
  - [ ] 全頁字串走 t()
  - [ ] 狀態顯示統一走 `common.status.*`（依賴 B5）
- **驗收**：4 頁切英文後無中文

---

## P2 — 可選（開發體驗 + 第三方語言）

### [ ] A7 `i18next-parser` + key 抽取腳本
- **檔案**：`frontend/i18next-parser.config.ts`、`frontend/package.json`
- **要做**：
  - [ ] 裝 `i18next-parser`
  - [ ] `npm run i18n:extract` 自動掃 `t()` call 產生缺 key 清單
  - [ ] 加 pre-commit hook
- **驗收**：新加 `t('foo.bar')` 但沒翻譯檔 → CI 警告

### [ ] A8 Translation staleness CI
- **檔案**：`.github/workflows/i18n_staleness.yml`、`scripts/check_translations.py`
- **要做**：
  - [ ] 比對各 locale JSON 的 key 覆蓋率
  - [ ] 缺 key > 閾值則 CI 失敗
  - [ ] 報表貼到 PR comment
- **驗收**：PR 動到 zh-TW 但沒改 en-US → CI 紅燈

### [ ] C1 新增 ja-JP 語言（跑 runbook 驗證）
- **檔案**：`frontend/src/i18n/locales/ja-JP/*.json`
- **要做**：
  - [ ] 補齊 common / conversation / ws / intake 四個 namespace
  - [ ] 臨床 / 醫學術語找母語者審核
  - [ ] 跑 B9 Playwright（切版本把預期字元改 hiragana / katakana）
- **驗收**：日本使用者全站零 fallback

### [ ] C2 新增 ko-KR 語言
- **檔案**：`frontend/src/i18n/locales/ko-KR/*.json`
- **要做**：同 C1，語言改韓文
- **驗收**：韓國使用者全站零 fallback

### [ ] C3 新增 vi-VN 語言
- **檔案**：`frontend/src/i18n/locales/vi-VN/*.json`
- **要做**：同 C1，注意越南文擴字（A6）
- **驗收**：越南使用者全站零 fallback

---

## 任務相依關係

```
A1 ──┬─ A2 ─┬─ A3 ─┬─ A4 ─┬─ B2 ✅
     │      │      │      └─ B3 ✅
     │      └─ B1  │
     │             └─ A9 ✅
     │
     ├─ A5 ─── A6
     │
     ├─ B4 ─┬─ B5 ─── B6
     │      └─ B7 ─── B8 ─── B9
     │
     └─ A7 ─── A8 ─── C1 / C2 / C3
```

- **可並行**：A5 / A6 / B4 / A7
- **可並行（依賴 B5）**：B6 / B7 / B8
- **最後收尾**：B9（Playwright 驗收）→ C1 / C2 / C3

---

## 優先推進建議

1. **先 B1**：seed 資料完成才能真的看到多語效果（阻塞 B9 / C1-C3）
2. **再 A5 + A6 並行**：前端效能與載入體驗（與 B 系列彼此獨立）
3. **再 B4 → B5**：format + badge（其他元件都會依賴）
4. **B6 / B7 / B8 並行**：頁面層 i18n
5. **B9 驗收 → C1/C2/C3 beta 語言**
