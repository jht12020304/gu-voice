# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
env/
.venv/

# Environment files (secrets)
.env
.env.local
.env.*.local
backend/.env
# Timestamped .env backups (含 live DB 密碼 / Supabase keys，禁止入庫)
.env*.bak*

# Logs
*.log
backend/backend.log

# Node
node_modules/
frontend/node_modules/

# Vite build output
frontend/dist/

# OS
.DS_Store
Thumbs.db

# PNG design screenshots (not needed in repo)
*.png
# Exception: app logo in frontend public
!frontend/public/logo.png
# Exception: iOS/Android 的**圖示資產**必須入庫。`*.png` 曾讓 AppIcon.appiconset 的
# 15 張圖永遠進不了版控，換一台機器 clone 出來就沒有圖示，而 actool 對「Contents.json
# 宣告了但檔案不在」不會 build error——Runner.app 只是安靜地生不出 Assets.car，一路
# 到 App Store Connect 上傳驗證才爆。
# ⚠️ 白名單刻意只開到 asset catalog／mipmap 這幾個目錄，**不要放寬成
# `!flutter_app/**/*.png`**：這是**公開** repo（github.com/jht12020304/gu-voice），
# 而這是醫療系統——一放寬，任何人隨手把醫師 dashboard 或 SOAP 報告的截圖放進
# flutter_app/ 就會被 commit 出去，且 git 歷史清不乾淨。
# build/ 是目錄層級排除，優先於這幾條，build 產物不會被拉進來。
!flutter_app/ios/Runner/Assets.xcassets/**/*.png
!flutter_app/android/app/src/main/res/**/*.png
!flutter_app/web/icons/*.png

# Apple 簽章金鑰與憑證——一旦 commit 就等於外洩，且 git 歷史清不乾淨。
# 目前這些檔案放在 repo 外的 ../firebase-secrets/，這幾行是防手滑複製進來。
*.p8
*.p12
*.cer
*.certSigningRequest
*.mobileprovision
AuthKey_*

# Misc design/integration notes
agent-skills-main/
awesome-design-md-main/
cal/
intercom/
notion/
sentry/
stripe/
supabase/
vercel/
# Vercel 環境匯出檔（含 secrets，禁止入庫）
vercel_*.yml

# Python test cache
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp

# graphify 知識圖譜產物（用 graphify CLI 重建即可，不入庫）
graphify-out/

# Local tool caches / scratch（.claude/skills 是專案技能，要入庫；其餘 .claude 內容不入庫）
.claude/*
!.claude/skills/
.playwright-cli/
.playwright-mcp/
output/
